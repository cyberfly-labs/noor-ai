PORT=8787
# Set to true only for testing against the pre-production environment
QF_USE_PRELIVE=false
# Client credentials for the Content API (client_credentials flow — backend only, never expose in mobile app)
QF_CLIENT_ID=your-quran-foundation-client-id
QF_CLIENT_SECRET=your-quran-foundation-client-secret
